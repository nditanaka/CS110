// This is not a valid program, it just contains style examples.

int main() {
    // The following has a few problems.
    // Inconsistent spacing around the operators
    // Variable names are vague
    while(a<= b&& a =>c) {

    }

    while(value <= max_value && value >= min_value) {

    }

  // This is not indented at the right level, which hurts readability
  for(int x = 0; x < 10; x++) {
  for(int y = 0; y < 10; y++) {

  }
  }

    // Some people like their curly braces like this:
    while(x > 10)
    {
        printf("%i\n", x);
        x--;
    }
    // Either style is fine, just be consistent!

    // Variable names should start with a lowercase letter.
    int this_is_snake_case;
    int thisIsCamelCase;
    // You can use either, but be consistent

    return 0;
}
